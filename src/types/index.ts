
export interface MainLayoutProps {
    children: React.ReactNode
}

export interface SeoProps { 
    title?: string
    slogan?: string
    description?: string
    isIndex?: boolean
    isBlank?: boolean
}

export interface PagePerfilProps {
    
}